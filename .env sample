# .env

# Redis

REDIS_PASSWORD="thisismypassword"
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379

# REDIS_URL=redis://localhost:6379 # if running without docker use this

# RPCs

RPC_HTTP_URL="https://your-own-rpc.com"
RPC_WS_URL="wss://your-own-rpc.com"
RPC_USE_OWN_ONLY=false

# Multi-server setup

PEER_COUNT=2 # number of instances running on different servers
PEER_INDEX=0 # index between 0 and PEER_COUNT-1

# Seed phrase

SEED_PHRASE="your twelve word mnemonic phrase"
