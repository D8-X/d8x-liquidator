{
  "dependencies": {
    "@d8x/perpetuals-sdk": "^0.0.20",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "pm2": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.9"
  },
  "scripts": {
    "postinstall": "$(yarn bin)/pm2 install typescript",
    "start": "pm2 start ecosystem.config.js --watch --log ./logs/liquidations.log",
    "stop": "pm2 stop all && pm2 delete all"
  }
}
